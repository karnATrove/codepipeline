{% extends 'WarehouseBundle::layout.html.twig' %}

{% block title %}
{{ parent() }} - Import product list
{% endblock %}

{% block body %}

    <div class="page-title">
      <div class="title_left">
        <h3>Import product list<span class="glyphicon glyphicon-file" aria-hidden="true"></span></h3>
      </div>
      <div class="title_right"></div>
    </div>

    <div class="clearfix"></div>

    <div class="row">
        <div class="col-md-12">
            {% for type, flashMessages in app.session.flashbag.all() %} 
            {% for flashMessage in flashMessages %}
            <div class="alert alert-{{ type }} alert-dismissible fade in">
                <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">×</span></button>
                {{ flashMessage|raw }}
            </div>
             {% endfor %} 
             {% endfor %} 
        </div>
    </div>

    
    <div class="row">
        <div class="col-xs-12">
            <div class="x_panel">
                <div class="x_title">
                  <h2>Import <small>Product for the incoming container</small></h2>
                  <ul class="nav navbar-right panel_toolbox">
                    <li><a class="collapse-link"><i class="fa fa-chevron-up"></i></a></li>
                    <li><a class="close-link"><i class="fa fa-close"></i></a></li>
                  </ul>
                  <div class="clearfix"></div>
                </div>
                <div class="x_content">
                    {{ form_start(form) }}

                    <div class="row">

                        <div class="col-xs-12 col-sm-6">
                            <div class="form-group {% if form_errors(form.filepath) %}item bad{% endif %}"">
                                {{ form_label(form.filepath) }}
                                {{ form_widget(form.filepath) }}
                            </div>
                        </div>

                        <div class="col-xs-12 col-sm-6">
                            {{ form_rest(form) }}
                        </div>

                    </div>
                    {{ form_widget(form) }}
                    <p>
                        <button type="submit" name="submit" value="save" class="btn btn-primary"> 
                            Save <span class="glyphicon glyphicon-ok-circle" aria-hidden="true"></span>
                        </button>
                    </p>


                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>

    
    

    <hr/>

    <div class="form-group">
        <a class="btn btn-default" href="{{ path('incoming_edit',{'id':incoming.id}) }}">
            <span class="glyphicon glyphicon-list" aria-hidden="true"></span>
            Back to container
        </a>
    </div>

    

</div>


{% endblock %}

